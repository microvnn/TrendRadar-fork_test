# ═══════════════════════════════════════════════════════════════
#                   Cấu hình Dòng thời gian TrendRadar
#                      Version: 1.0.0
# ═══════════════════════════════════════════════════════════════
#
# Tệp này kiểm soát "làm gì vào thời gian nào".
#
# Hầu hết mọi người không cần chỉnh sửa tệp này.
# Chỉ cần chọn một mẫu cài đặt sẵn trong config.yaml:
#
#   schedule:
#     preset: "morning_evening"    ← Sửa ở đây là được
#
#
# Địa chỉ trình chỉnh sửa cấu hình trực quan: https://sansan0.github.io/TrendRadar/
#
#
# ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
# 📖 Khái niệm cơ bản (giúp bạn hiểu cấu hình bên dưới)
# ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
#
#
# 🔁 Chương trình hoạt động như thế nào?
#
#   TrendRadar không chạy ngầm liên tục mà được đánh thức định kỳ bởi "đồng hồ báo thức":
#
#     Người dùng GitHub Actions → Được kích hoạt bởi cron trong .github/workflows/crawler.yml
#                               Mặc định chạy mỗi giờ một lần (ví dụ: phút thứ 33 mỗi giờ)
#
#     Người dùng Docker         → Được kích hoạt bởi CRON_SCHEDULE trong docker/.env
#                               Mặc định chạy mỗi 30 phút một lần
#
#   Mỗi khi được đánh thức, chương trình thực hiện theo 3 giai đoạn:
#
#     1️⃣ Thu thập (collect)
#        Thu thập dữ liệu mới nhất từ các nền tảng hot + nguồn RSS, lưu vào cơ sở dữ liệu
#
#                  ⬇
#
#     2️⃣ Phân tích (analyze)
#        Gọi mô hình AI lớn để phân tích sâu tin tức đã thu thập (tùy chọn, cần cấu hình API Key)
#
#                  ⬇
#
#     3️⃣ Đẩy tin (push)
#        Gửi tin tức nóng đã tổng hợp + kết quả phân tích AI đến kênh thông báo của bạn
#        (Lark, DingTalk, Telegram, Email, v.v.)
#
#   Cả 3 giai đoạn này đều có thể bật/tắt độc lập. Tác dụng của tệp này là kiểm soát:
#   "Trong khoảng thời gian nào, bật/tắt những giai đoạn nào".
#
#
# 🔌 Mối quan hệ giữa Công tắc tổng trong config.yaml và Công tắc thời gian trong timeline
#
#   Trong config.yaml có một vài "công tắc tổng", độ ưu tiên cao hơn tệp này:
#
#     platforms.enabled: false   → Không bao giờ thu thập Hot Search (bất kể timeline cài đặt ra sao)
#     rss.enabled: false         → Không bao giờ thu thập RSS (tương tự như trên)
#     notification.enabled: false → Không bao giờ đẩy tin (tương tự như trên)
#     ai_analysis.enabled: false  → Không bao giờ phân tích (tương tự như trên)
#
#   Chỉ khi công tắc tổng là true, công tắc thời gian trong timeline mới có hiệu lực.
#   Nói cách khác: Công tắc tổng quyết định "có được làm không", timeline quyết định "khi nào làm".
#
#
# ⏰ "Khoảng thời gian" và "Giai đoạn yên lặng" là gì?
#
#   Bạn có thể tưởng tượng một ngày là một dòng thời gian, trên đó chia thành các "khoảng thời gian".
#   Mỗi khoảng thời gian có công tắc hành vi riêng (có thu thập không, có phân tích không, có đẩy tin không).
#
#   Thời gian không nằm trong bất kỳ khoảng nào gọi là "Giai đoạn yên lặng" (chạy theo cấu hình default).
#   Giai đoạn yên lặng thường bắt buộc phải thu thập, để dữ liệu được tích lũy liên tục,
#   đợi đến lúc đẩy tin sẽ tổng hợp được báo cáo đầy đủ.
#
#
#   💡 Giai đoạn yên lặng càng dài, dữ liệu tích lũy càng phong phú (biến động xếp hạng, thời gian lên/xuống top,...),
#   ngữ cảnh gửi cho AI phân tích cũng càng đầy đủ, chất lượng phân tích càng cao.
#   So với MCP Server, dữ liệu cả ngày của phương án này thể hiện rõ ràng hơn xu hướng và mạch thay đổi.
#
#
# ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
# 📋 Danh sách mẫu cài đặt sẵn (chọn một là được)
# ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
#
#   1️⃣ always_on        Giám sát 24/7, có mới là đẩy (Mặc định)
#   2️⃣ morning_evening  Đẩy tin cả ngày + Tổng hợp tối (Khuyên dùng cho đa số)
#   3️⃣ office_hours     3 khung giờ làm việc: Đầu giờ → Trưa → Tan làm
#   4️⃣ night_owl        Lướt tin chiều + Tổng hợp đêm khuya
#   5️⃣ custom           Tùy chỉnh hoàn toàn (cần sửa phần custom ở cuối tệp này)
#
# Muốn tùy chỉnh? Hai cách:
#   1. Kéo xuống phần "Chế độ tùy chỉnh" ở cuối tệp này
#   2. Thêm mẫu cài đặt của riêng bạn vào danh sách presets bên dưới
#      (Miễn là key không trùng, sau đó điền tên mẫu của bạn vào config.yaml)
#
# ⚠️ Gợi ý về thiết lập khoảng thời gian:
#   GitHub Actions: Khuyên dùng khoảng cách task định kỳ ≥ 2 giờ. Do hệ thống kích hoạt có độ trễ ngẫu nhiên, khoảng cách quá ngắn có thể khiến task bị bỏ qua.
#   Người dùng Docker: Cron định kỳ rất chuẩn xác, không có hạn chế này, cứ cài đặt theo nhu cầu.
#
#
# ═══════════════════════════════════════════════════════════════


# ───────────────────────────────────────────────────────────────
# Mẫu cài đặt sẵn
# ───────────────────────────────────────────────────────────────
presets:

  # ───────────────────────────────────────────────────────────
  # 1️⃣ always_on - Giám sát toàn thời gian
  #
  # Chế độ đơn giản nhất: Thu thập + Đẩy tin 24/7, có mới là báo.
  # Không chia khung giờ, cả ngày dùng chung một cấu hình.
  # Phù hợp: Người dùng cường độ cao, giám sát dư luận thời gian thực
  #
  # Cả ngày: Đẩy tin ✓ | AI Phân tích ✗ | Không giới hạn số lần đẩy
  # ───────────────────────────────────────────────────────────
  always_on:
    name: "Giám sát toàn thời gian"
    description: "Giám sát 24/7, đẩy tin ngay khi có mới. Thích hợp cho người dùng cường độ cao."

    # Cấu hình mặc định ── Khi không trong khoảng thời gian nào, dùng các công tắc này
    # Vì chế độ này không chia khung giờ, nên default chính là hành vi cả ngày
    default:
      collect: true                # Thu thập dữ liệu (Hot Search + RSS)
      analyze: false               # Không phân tích AI (tiết kiệm phí API)
      ai_mode: "current"           # AI phân tích bảng xếp hạng hiện tại
      push: true                   # Có nội dung mới là đẩy
      report_mode: "incremental"   # Chỉ đẩy nội dung mới tăng thêm, tránh lặp lại
      once:                        # Giới hạn chỉ thực hiện một lần trong mỗi khoảng thời gian
        analyze: false             #   Không giới hạn số lần phân tích
        push: false                #   Không giới hạn số lần đẩy tin

    # Không định nghĩa khoảng thời gian nào, cả ngày đều chạy default
    #
    # Gợi ý cú pháp: {} là cách viết "từ điển rỗng" trong YAML, nghĩa là bên trong không có gì.
    # Tương đương với viết nhiều dòng nhưng không điền gì. [] phía sau cũng vậy, nghĩa là "danh sách rỗng".
    periods: {}
    day_plans:
      all_day:
        periods: []                   # Danh sách rỗng = Ngày hôm đó không bật khung giờ nào
    week_map:
      1: "all_day"                 # Thứ 2
      2: "all_day"                 # Thứ 3
      3: "all_day"                 # Thứ 4
      4: "all_day"                 # Thứ 5
      5: "all_day"                 # Thứ 6
      6: "all_day"                 # Thứ 7
      7: "all_day"                 # Chủ nhật


  # ───────────────────────────────────────────────────────────
  # 2️⃣ morning_evening - Tổng hợp Sáng Tối (Khuyên dùng)
  #
  # Cả ngày đẩy tin nóng hiện tại + Buổi tối làm một bản tổng hợp cả ngày.
  # Phù hợp: Đa số mọi người
  #
  # Mặc định (Cả ngày): Đẩy tin ✓ | AI Phân tích ✓ | Không giới hạn số lần đẩy
  # Tổng hợp tối: Đẩy tin ✓ | AI Phân tích ✓ | Chỉ đẩy/phân tích 1 lần
  # ───────────────────────────────────────────────────────────
  morning_evening:
    name: "Tổng hợp Sáng Tối"
    description: "Đẩy tin cả ngày + Tổng hợp tối. Phù hợp đa số mọi người."

    # Cấu hình mặc định ── Hành vi khi không trúng khung giờ nào
    default:
      collect: true                # Luôn thu thập
      analyze: true                # AI phân tích bảng xếp hạng hiện tại
      ai_mode: "current"           # AI phân tích bảng xếp hạng hiện tại
      push: true                   # Mỗi lần đều đẩy tin nóng hiện tại
      report_mode: "current"       # Tin tức đang trên bảng xếp hạng
      once:
        analyze: false             # Không giới hạn số lần phân tích
        push: false                # Không giới hạn số lần đẩy tin

    # Định nghĩa khung giờ ── Chỉ có tổng hợp tối cần xử lý đặc biệt
    periods:
      evening_summary:
        name: "Tổng hợp tối"
        start: "20:00"
        end: "22:00"
        analyze: true              # Buổi tối chạy phân tích AI
        ai_mode: "daily"           # AI tổng hợp nội dung cả ngày
        report_mode: "daily"       # Chuyển sang chế độ tổng hợp toàn bộ tin tức trong ngày
        once:
          analyze: true            # Trong khung giờ này chỉ phân tích 1 lần
          push: true               # Trong khung giờ này chỉ đẩy tin 1 lần

    # Kế hoạch ngày ── Lắp ghép các khung giờ thành lịch trình 1 ngày
    day_plans:
      all_day:
        periods: ["evening_summary"]

    # Ánh xạ tuần ── Mỗi ngày dùng kế hoạch nào (1=Thứ 2 ... 7=Chủ nhật)
    week_map:
      1: "all_day"
      2: "all_day"
      3: "all_day"
      4: "all_day"
      5: "all_day"
      6: "all_day"
      7: "all_day"


  # ───────────────────────────────────────────────────────────
  # 3️⃣ office_hours - Đẩy tin giờ hành chính
  #
  # Ngày làm việc chia 3 đợt đẩy, cuối tuần đẩy tăng thêm tự do.
  # Phù hợp: Dân văn phòng, người dùng doanh nghiệp
  #
  # Mặc định (Yên lặng): Đẩy tin ✗ | AI Phân tích ✗
  # Điểm tin đầu giờ: Đẩy tin ✓ | AI Phân tích ✓ | Chỉ đẩy 1 lần
  # Tin nóng buổi trưa: Đẩy tin ✓ | AI Phân tích ✗ | Chỉ đẩy 1 lần
  # Tổng hợp tan làm: Đẩy tin ✓ | AI Phân tích ✓ | Chỉ đẩy 1 lần
  # Cuối tuần tự do: Đẩy tin ✓ | AI Phân tích ✗ | Không giới hạn số lần đẩy
  # ───────────────────────────────────────────────────────────
  office_hours:
    name: "Giờ hành chính"
    description: "Ngày làm việc đẩy 3 đợt (Đầu giờ→Trưa→Tan làm), cuối tuần đẩy tăng thêm tự do."

    default:
      collect: true
      analyze: false
      ai_mode: "current"
      push: false                  # Mặc định không đẩy
      report_mode: "current"
      once:
        analyze: true              # Mỗi khung giờ chỉ phân tích 1 lần
        push: true                 # Mỗi khung giờ chỉ đẩy 1 lần

    periods:
      morning_briefing:
        name: "Điểm tin đầu giờ"
        start: "09:00"
        end: "11:00"
        analyze: true              # AI phân tích tin nóng hiện tại
        ai_mode: "current"         # AI phân tích bảng xếp hạng hiện tại
        push: true                 # Đến công ty xem tin nóng
        report_mode: "current"     # Tin tức đang trên bảng xếp hạng
        # once kế thừa default (analyze: true, push: true) → Chỉ đẩy/phân tích 1 lần

      noon_update:
        name: "Tin nóng buổi trưa"
        start: "13:00"
        end: "15:00"              
        push: true                 # Buổi trưa đẩy tin nóng hiện tại
        report_mode: "current"     # Tin tức đang trên bảng xếp hạng
        # analyze kế thừa default: false → Trưa không phân tích AI, tiết kiệm API
        # once kế thừa default (push: true) → Chỉ đẩy 1 lần

      closing_summary:
        name: "Tổng hợp tan làm"
        start: "17:00"
        end: "19:00"
        analyze: true              # AI làm tổng hợp cả ngày
        ai_mode: "daily"           # AI phân tích nội dung cả ngày
        push: true                 # Trước khi về đẩy tổng hợp đầy đủ
        report_mode: "daily"       # Tổng hợp toàn bộ tin tức trong ngày
        # once kế thừa default (analyze: true, push: true) → Chỉ đẩy/phân tích 1 lần

      weekend_free:
        name: "Cuối tuần tự do"
        start: "08:00"
        end: "23:00"
        ai_mode: "current"         # AI phân tích bảng xếp hạng hiện tại
        push: true                 # Có mới là đẩy
        report_mode: "incremental" # Chế độ tăng thêm: có mới mới đẩy, không thì im lặng
        once:
          analyze: false           # Không giới hạn số lần phân tích
          push: false              # Không giới hạn số lần đẩy

    # Ngày làm việc dùng chế độ 3 đợt; Cuối tuần dùng chế độ tăng thêm tự do
    day_plans:
      workday:
        periods: ["morning_briefing", "noon_update", "closing_summary"]
      weekend:
        periods: ["weekend_free"]  # Cuối tuần: có mới thì đẩy, không làm phiền giấc ngủ

    week_map:
      1: "workday"                 # Thứ 2 → Kế hoạch ngày làm việc
      2: "workday"
      3: "workday"
      4: "workday"
      5: "workday"
      6: "weekend"                 # Thứ 7 → Kế hoạch cuối tuần
      7: "weekend"                 # Chủ nhật → Kế hoạch cuối tuần


  # ───────────────────────────────────────────────────────────
  # 4️⃣ night_owl - Chế độ Cú đêm
  #
  # Ban ngày yên tĩnh, chiều và đêm khuya mỗi buổi đẩy 1 lần.
  # Phù hợp: Người làm việc ban đêm, người dùng lệch múi giờ, freelancer
  #
  # Mặc định (Ban ngày yên lặng): Đẩy tin ✗ | AI Phân tích ✗
  # Lướt tin chiều: Đẩy tin ✓ | AI Phân tích ✓ | Chỉ đẩy 1 lần
  # Tổng hợp đêm khuya: Đẩy tin ✓ | AI Phân tích ✓ | Chỉ đẩy 1 lần
  # ───────────────────────────────────────────────────────────
  night_owl:
    name: "Chế độ Cú đêm"
    description: "Lướt tin chiều + Tổng hợp đêm khuya. Phù hợp người làm đêm, lệch múi giờ."

    default:
      collect: true
      analyze: false
      ai_mode: "current"
      push: false
      report_mode: "current"
      once:
        analyze: true              # Mỗi khung giờ chỉ phân tích 1 lần
        push: true                 # Mỗi khung giờ chỉ đẩy 1 lần

    periods:
      afternoon_peek:
        name: "Lướt tin chiều"
        start: "15:00"
        end: "17:00"
        analyze: true              # AI phân tích tin nóng hiện tại
        ai_mode: "current"         # AI phân tích bảng xếp hạng hiện tại
        push: true                 # Chiều xem tin nóng
        report_mode: "current"     # Tin tức đang trên bảng xếp hạng
        # once kế thừa default (analyze: true, push: true) → Chỉ đẩy/phân tích 1 lần

      late_night:
        name: "Tổng hợp đêm khuya"
        start: "22:00"
        end: "01:00"               # start > end → Tự động nhận diện là qua ngày hôm sau
        analyze: true              # AI làm tổng hợp cả ngày
        ai_mode: "daily"           # AI phân tích nội dung cả ngày
        push: true                 # Đêm khuya đẩy tổng hợp đầy đủ
        report_mode: "daily"       # Tổng hợp toàn bộ tin tức trong ngày
        # once kế thừa default (analyze: true, push: true) → Chỉ đẩy/phân tích 1 lần

    day_plans:
      all_day:
        periods: ["afternoon_peek", "late_night"]
    week_map:
      1: "all_day"
      2: "all_day"
      3: "all_day"
      4: "all_day"
      5: "all_day"
      6: "all_day"
      7: "all_day"


# ═══════════════════════════════════════════════════════════════
#
# 5️⃣ Chế độ tùy chỉnh
#
# Khi trong config.yaml đặt schedule.preset: "custom",
# hệ thống sẽ đọc đoạn cấu hình bên dưới này.
#
# Nếu các mẫu cài đặt sẵn ở trên không đáp ứng nhu cầu, bạn có thể tự do định nghĩa tại đây.
#
# ═══════════════════════════════════════════════════════════════
#
# Tư duy cấu hình rất đơn giản, giống như xếp hình:
#
#   Bước 1: Định nghĩa "Khối gạch" (periods)
#            Mỗi khối = Một khoảng thời gian + Làm gì trong thời gian đó
#            Ví dụ: Sáng 08-10 Đẩy tin, Tối 19-21 Tổng hợp
#
#   Bước 2: Ghép thành "Lịch trình một ngày" (day_plans)
#            Kết hợp các khối gạch lại tạo thành lịch trình trong ngày
#            Ví dụ: Ngày làm việc dùng [Sáng, Tối], Cuối tuần dùng [Tối]
#
#   Bước 3: Chỉ định "Ngày nào dùng lịch nào" (week_map)
#            Từ thứ 2 đến Chủ nhật, mỗi ngày tương ứng với kế hoạch nào
#            Ví dụ: T2~T6 dùng workday, T7 CN dùng weekend
#
#   Ngoài ra còn một "Cấu hình mặc định" (default),
#   khi một thời điểm không nằm trong bất kỳ khối gạch nào, sẽ dùng cấu hình mặc định.
#   Các trường không ghi trong khối gạch cũng sẽ tự động lùi về dùng cấu hình mặc định.
#
#
# Dưới đây là một ví dụ tùy chỉnh hoàn chỉnh, ngày làm việc và cuối tuần dùng khung giờ khác nhau:
#
#   Khung giờ ngày làm việc:
#     Đêm yên lặng 23:00-06:00 (qua ngày): Thu thập ✓ | Phân tích ✓ | Đẩy tin ✗
#     Sáng ngày làm việc 08:00-10:00: Đẩy tin ✓ | incremental
#     Tổng hợp tối 19:00-21:00: Đẩy tin ✓ | Phân tích ✓ | daily
#     Thời gian còn lại chạy theo default (Duy trì thu thập)
#
#   Khung giờ cuối tuần:
#     Đêm yên lặng 23:00-06:00 (qua ngày): Thu thập ✓ | Phân tích ✓ | Đẩy tin ✗
#     Sáng cuối tuần 10:00-12:00: Đẩy tin ✓ | daily
#     Tổng hợp tối 19:00-21:00: Đẩy tin ✓ | Phân tích ✓ | daily
#     Thời gian còn lại chạy theo default (Duy trì thu thập)

custom:
  name: "Tùy chỉnh"
  description: "Tự do định nghĩa khung giờ, kế hoạch ngày và ánh xạ tuần."

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Cấu hình mặc định
  #
  # Khi thời điểm hiện tại không nằm trong bất kỳ khung giờ (khối gạch) nào, sẽ dùng bộ công tắc này.
  # Các trường không được viết trong khung giờ cũng sẽ lùi về đây.
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  default:
    collect: true                  # Có thu thập dữ liệu không (Hot Search + RSS)
    analyze: false                 # Có chạy phân tích AI không
    ai_mode: "current"            # Chế độ AI phân tích:
                                   #   follow_report → Theo report_mode
                                   #   daily         → Bắt buộc tổng hợp cả ngày
                                   #   current       → Bắt buộc theo bảng hiện tại
                                   #   incremental   → Bắt buộc chế độ tăng thêm
    push: false                    # Có gửi thông báo đẩy không
    report_mode: "current"         # Chế độ báo cáo:
                                   #   daily       → Tổng hợp toàn bộ tin trong ngày
                                   #   current     → Tin tức đang trên bảng xếp hạng
                                   #   incremental → Chỉ đẩy nội dung mới
    once:
      analyze: true                # Trong khung giờ này chỉ phân tích 1 lần (tiết kiệm API)
      push: true                   # Trong khung giờ này chỉ đẩy 1 lần (đỡ phiền)


  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Bước 1: Định nghĩa Khối gạch (Khung giờ)
  #
  # Mỗi khung giờ có một key duy nhất (như deep_quiet),
  # và start / end biểu thị phạm vi hiệu lực.
  #
  # Chỉ cần viết "các trường khác với default", còn lại tự động kế thừa default.
  # Ví dụ weekday_morning không viết collect, sẽ kế thừa collect: true của default.
  #
  # Mẹo: Nếu start > end (như 22:00 → 07:00),
  #       hệ thống tự động nhận diện là khung giờ qua đêm.
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  periods:

    deep_quiet:
      name: "Đêm yên lặng"
      start: "23:00"
      end: "06:00"                 # 23:00 → 06:00 hôm sau (khung giờ qua ngày)
      collect: true                # Ban đêm tiếp tục thu thập dữ liệu
      analyze: true                # Ban đêm có thể chạy phân tích AI (đằng nào cũng không đẩy)
      push: false                  # Đêm khuya không đẩy, tránh làm phiền

    weekday_morning:
      name: "Sáng ngày làm việc"
      start: "08:00"
      end: "10:00"                 # Độ rộng 2h, trừ hao thời gian kích hoạt
      push: true                   # Sáng đẩy 1 lần
      report_mode: "incremental"   # Chỉ đẩy nội dung mới
      # once kế thừa default (push: true) → Trong cửa sổ này chỉ đẩy 1 lần

    weekend_morning:
      name: "Sáng cuối tuần"
      start: "10:00"
      end: "12:00"                 # Độ rộng 2h
      push: true
      report_mode: "daily"         # Cuối tuần xem tổng hợp cả ngày
      # once kế thừa default (push: true) → Trong cửa sổ này chỉ đẩy 1 lần

    evening_summary:
      name: "Tổng hợp tối"
      start: "19:00"
      end: "21:00"
      analyze: true                # Tối chạy phân tích AI
      ai_mode: "daily"             # AI phân tích nội dung cả ngày
      push: true                   # Tối đẩy tin
      report_mode: "daily"         # Tổng hợp toàn bộ tin trong ngày
      # once kế thừa default (analyze: true, push: true) → Chỉ phân tích/đẩy 1 lần


  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Bước 2: Ghép khối gạch thành Kế hoạch ngày
  #
  # Tổ hợp các khung giờ định nghĩa ở trên thành lịch trình một ngày.
  # Bạn có thể định nghĩa nhiều kế hoạch ngày (như workday và weekend),
  # sau đó phân bổ cho các thứ trong tuần ở Bước 3 (week_map).
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  day_plans:
    workday:                       # Kế hoạch ngày làm việc
      periods: ["deep_quiet", "weekday_morning", "evening_summary"]
    weekend:                       # Kế hoạch cuối tuần (thay sáng làm việc bằng sáng cuối tuần)
      periods: ["deep_quiet", "weekend_morning", "evening_summary"]


  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Bước 3: Chỉ định ngày nào dùng kế hoạch nào
  #
  # 1=Thứ 2  2=Thứ 3  3=Thứ 4  4=Thứ 5  5=Thứ 6  6=Thứ 7  7=Chủ nhật
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  week_map:
    1: "workday"                   # Thứ 2 → Kế hoạch ngày làm việc
    2: "workday"                   # Thứ 3
    3: "workday"                   # Thứ 4
    4: "workday"                   # Thứ 5
    5: "workday"                   # Thứ 6
    6: "weekend"                   # Thứ 7 → Kế hoạch cuối tuần
    7: "weekend"                   # Chủ nhật


  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Chiến lược xung đột (Thường không cần sửa)
  #
  # "Xung đột" là gì?
  #   Nếu hai khung giờ của bạn chồng lên nhau (ví dụ A là 08:00-12:00, B là 10:00-14:00),
  #   thì khoảng 10:00-12:00 đồng thời thuộc về cả A và B, gây ra xung đột.
  #   Lúc này chương trình cần biết: Phải nghe theo ai?
  #
  # Hai cách xử lý:
  #
  #   error_on_overlap (Khuyên dùng)
  #     Báo lỗi trực tiếp, nhắc bạn sửa cấu hình.
  #     Phù hợp đại đa số —— Trùng lặp thường do viết nhầm, báo lỗi đễ phát hiện sớm.
  #
  #   last_wins
  #     Trong danh sách periods của day_plans, cái nào viết sau thì ưu tiên.
  #     Ví dụ periods: ["A", "B"], khi trùng lặp thì B có hiệu lực.
  #     Phù hợp: Bạn muốn dùng một khung giờ phạm vi lớn làm nền, rồi dùng khung giờ nhỏ đè lên.
  #
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  overlap:
    policy: "error_on_overlap"
